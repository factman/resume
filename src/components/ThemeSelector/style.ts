import {makeStyles} from '@material-ui/core/styles';

export default makeStyles({
  root: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  label: {
    flex: 1,
    margin: '5px 0 5px 11px',
  },
});
